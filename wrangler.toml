name = "agroconecta"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Nota: As imagens estão no Supabase Storage (bucket 'property-images').
# O Cloudflare R2 não é usado neste projecto.
